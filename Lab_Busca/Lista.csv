#include <stdio.h>

int busca_binaria_recursiva(int lista[], int alvo, int inicio, int fim) {
    if (inicio > fim) {
        return -1;  // Elemento não encontrado
    }

    // Calcula o índice do elemento do meio
    int meio = (inicio + fim) / 2;

    // Compara o elemento do meio com o alvo
    if (lista[meio] == alvo) {
        return meio;  // Elemento encontrado
    } else if (lista[meio] < alvo) {
        // Se o elemento do meio for menor que o alvo, busca na metade direita
        return busca_binaria_recursiva(lista, alvo, meio + 1, fim);
    } else {
        // Se o elemento do meio for maior que o alvo, busca na metade esquerda
        return busca_binaria_recursiva(lista, alvo, inicio, meio - 1);
    }
}